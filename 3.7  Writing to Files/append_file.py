# Functions r(read), W(Write), a(Append), r+(Read and Write)

employe_files = open("employees.txt", "a") # From this we can write something on that file.

employe_files.write("\nToby - Human Resource") #It will wite to the file.What we have written in it.

employe_files.close() # It will close the file that we have oppened.