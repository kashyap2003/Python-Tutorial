# Functions r(read), W(Write), a(Append), r+(Read and Write)

employe_files = open("index.html", "w") # From this we can overwrite file

employe_files.write("<h3>Kashyap</h3>") #It will over write everything in file.

employe_files.close() # It will close the file that we have oppened.